#include <stdio.h>

void main(){
	int num1, a,b,c;
	printf("Escolha um numero inteiro: ");
	scanf("%d",&num1);
	
	if((num1>=100) && (num1<=999)){
		a = n1/100;
		b = (n1/10)%10;
		c= n1%10;
		if(a<=b && a<=c){
			n2=a;
			if(){
			}
		}
	}else{
		printf("Numero invalodo");
	}
}
